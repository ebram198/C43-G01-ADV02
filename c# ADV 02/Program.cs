namespace c__ADV_02
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region 1- You are given an ArrayList containing a sequence of elements. try to reverse the order of elements in the ArrayList in-place(in the same arrayList) without using the builin Reverse. Implement a function that takes the ArrayList as input and modifies it to have the reversed order of elements.-


            //CustomArrayList customArrayList = new CustomArrayList();
            //customArrayList.Add(1);
            //customArrayList.Add(2);
            //customArrayList.Add(3);
            //customArrayList.Add(4);
            //customArrayList.Add(5);
            //customArrayList.Add(6);

            //Console.WriteLine("befor revers List:");
            //customArrayList.Print();

            //customArrayList.Reverse();

            //Console.WriteLine("after reverse List:");
            //customArrayList.Print();




            #endregion



            #region 2-You are given a list of integers. Your task is to find and return a new list containing only the even numbers from the given list.

            //List<int> numbers = new List<int> { 1, 2, 5, 7, 9, 8, 3, 6, 15, 18, 10 };


            //List<int> evenNumbers = new List<int>();


            //for (int i = 0; i < numbers.ToArray().Length; i++)
            //{
            //    if (numbers[i] % 2 == 0)
            //    {
            //        evenNumbers.Add(numbers[i]);
            //    }
            //}

            //Console.WriteLine("Even Numbers: ");
            //foreach (int evenNumber in evenNumbers)
            //{
            //    Console.Write(evenNumber + " ");
            //}

            #endregion



            #region 3-implement a custom list called FixedSizeList<T> with a predetermined capacity. This list should not allow more elements than its capacity and should provide clear messages if one tries to exceed it or access invalid indices.

            //FixedSizeList<int> fixedList = new FixedSizeList<int>(6);

            //fixedList.Add(1);
            //fixedList.Add(2);
            //fixedList.Add(3);
            //fixedList.Add(4);
            //fixedList.Add(5);
            //fixedList.Add(6);

            //Console.WriteLine($"Element at index 1 is: {fixedList.Get(1)}");
            //Console.WriteLine($"Element at index 4 is: {fixedList.Get(4)}");


            #endregion




        }
    }
}